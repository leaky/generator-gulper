language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-gulper' ]; then cd .. && eval "mv $currentfolder generator-gulper" && cd generator-gulper; fi

